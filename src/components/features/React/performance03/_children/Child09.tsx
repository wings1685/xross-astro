/** @jsxImportSource react */

import { Child10 } from "./";
import type { RelayProps } from "../_models/types";

export default function Child09(props: RelayProps) {
	return <Child10 relay={ props.relay } setRelay={ props.setRelay } />;
};
